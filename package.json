{
  "name": "proyecto-final",
  "version": "1.1.0",
  "private": true,
  "engines": {
    "node": ">=16.x"
  },
  "scripts": {
    "start": "cd src/backend && npm start",
    "build": "",
    "install": "cd src/backend && npm install"
  }
}